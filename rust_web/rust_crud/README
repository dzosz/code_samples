based on https://github.com/olajohn-ajiboye/Rust-Rest-API

Instructions:
$ cargo install diesel_cli --no-default-features --features sqlite
$ diesel migration run 
$ cargo build
$ source my_env && cargo run
